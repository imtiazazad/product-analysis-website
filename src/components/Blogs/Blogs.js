import React from 'react';

const Blogs = () => {
    return (
        <div>
            <h2>What is context api? why we use it?</h2>
            <p>Context api is one of the most important topic in react. Generally </p>
        </div>
    );
};

export default Blogs;